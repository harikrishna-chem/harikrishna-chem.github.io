{
  "name": "harikrishna-sahu-personal-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port=8080",
    "clean": "rm -rf _site .eleventy-cache"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
